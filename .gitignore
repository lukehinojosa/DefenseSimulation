# Build output
build/
build-*/
cmake-build-*/
out/

# CMake
CMakeCache.txt
CMakeFiles/
_deps/

# IDE
.idea/
.vs/
*.user

# Compiled objects / binaries
*.o
*.obj
*.a
*.exe
*.out

# OSM/DoITT/Overture fetch cache + raw responses (regenerate via the tools/*.py
# fetch scripts). The baked asset assets/un_map.bin IS committed; its raw inputs
# are not. tools/tiles/ holds bld_*, part_*, and nyc_* tiles.
tools/tiles/
tools/overture_heights.csv
tools/*.log

# Diagnostic telemetry dumps written by defense_sim's SIM_LOG env (see main.cpp).
simlog*.csv
*.simlog.csv
sim_log*.csv
un_coast*.json
un_buildings*.json
un_raw.json
un_water.json
*_try.json
